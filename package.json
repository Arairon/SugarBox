{
  "$schema": "https://json.schemastore.org/package",
  "name": "sugarbox",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:container:server": "docker build -t ghcr.io/arairon/sugarbox -f docker/server.Dockerfile ."
  }
}